# Load the AppleTalk (DDP) kernel module at boot.
# Installed to /etc/modules-load.d/appletalk.conf by setup.sh.
# Required by atalkd/papd. Needs a kernel built with CONFIG_ATALK (=m in
# Raspberry Pi OS / Debian 13, kernel 6.18).
appletalk
